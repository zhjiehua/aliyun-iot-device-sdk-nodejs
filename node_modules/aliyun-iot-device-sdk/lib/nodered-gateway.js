
var util = require('util');
var Gateway = require('./gateway');

var _require = require('./utils'),
    createGuid = _require.createGuid,
    hmacSign = _require.hmacSign,
    signUtil = _require.signUtil;

var guid = createGuid();

var NodeRedGateway = function (_Gateway) {
  _inherits(NodeRedGateway, _Gateway);

  function NodeRedGateway() {
    _classCallCheck(this, NodeRedGateway);

    return _possibleConstructorReturn(this, (NodeRedGateway.__proto__ || Object.getPrototypeOf(NodeRedGateway)).apply(this, arguments));
  }

  _createClass(NodeRedGateway, [{
    key: 'addTopo',
    value: function addTopo(deviceConfig, cb) {
      var deviceSign = signUtil(deviceConfig);
      _get(NodeRedGateway.prototype.__proto__ || Object.getPrototypeOf(NodeRedGateway.prototype), 'addTopo', this).call(this, deviceSign, cb);
    }
  }, {
    key: 'login',
    value: function login(deviceConfig, cb) {
      var deviceSign = signUtil(deviceConfig);
      _get(NodeRedGateway.prototype.__proto__ || Object.getPrototypeOf(NodeRedGateway.prototype), 'login', this).call(this, deviceSign, cb);
    }
  }]);

  return NodeRedGateway;
}(Gateway);

module.exports = NodeRedGateway;